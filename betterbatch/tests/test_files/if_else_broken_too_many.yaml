variables:
 my_file: <__script_path__>\if_else_checks.yaml

broken_too_many:
- If:
    - Exists: <my_file>_
    - Do  :
        echo "BROKEN exists <my_file>
    - Else:
        echo string_else
    - run:
        shouldn't be here!
